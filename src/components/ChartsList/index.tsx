import './charts.css'
import Chart from "../Chart"

const ChartsList = () => {
  return (
    <div id="chart-list">
      <Chart data={[]}/>
      <Chart data={[]}/>
      <Chart data={[]}/>
      <Chart data={[]}/>
    </div>
  )

}

export default ChartsList
